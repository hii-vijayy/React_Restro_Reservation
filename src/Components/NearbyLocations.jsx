import { useState } from 'react';

function NearbyLocations() {
    const [state, setState] = useState();

    return (
        <div>
            
        </div>
    );
}

export default NearbyLocations; // Corrected export name
